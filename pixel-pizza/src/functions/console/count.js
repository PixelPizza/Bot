'use strict';

/**
 * Counts a label (shorter version of console.count)
 * @param {string} label The label to use
 * @returns {void}
 */
const count = label => console.count(label);

module.exports = count;